<UserControl x:Class="RushRacing.Kiosk.Views.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A1A">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="⚠️"
                       FontSize="64"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <TextBlock Text="TEMPORARILY UNAVAILABLE"
                       Foreground="#FF3333"
                       FontSize="32"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <TextBlock x:Name="ErrorMessageText"
                       Text="This machine is experiencing a problem."
                       Foreground="#888888"
                       FontSize="18"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,0,0,30"/>

            <TextBlock Text="Problems? WhatsApp: +60XX-XXXXXXX"
                       Foreground="#555555"
                       FontSize="16"
                       HorizontalAlignment="Center"/>

        </StackPanel>
    </Grid>
</UserControl>