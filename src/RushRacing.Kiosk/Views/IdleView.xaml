<UserControl x:Class="RushRacing.Kiosk.Views.IdleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A1A">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="🏁"
                       FontSize="72"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <TextBlock Text="RUSH RACING"
                       Foreground="#FF3333"
                       FontSize="60"
                       FontWeight="Bold"
                       FontFamily="Segoe UI"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <TextBlock Text="SCAN TO RACE"
                       Foreground="White"
                       FontSize="32"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <!-- QR Code Image -->
            <Border Background="White"
                    CornerRadius="12"
                    Padding="16"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,30">
                <Image x:Name="QrCodeImage"
                       Width="250" Height="250"/>
            </Border>

            <TextBlock x:Name="BookingUrlText"
                       Foreground="#888888"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>

            <TextBlock Text="Starting from RM10"
                       Foreground="#888888"
                       FontSize="22"
                       HorizontalAlignment="Center"/>

        </StackPanel>
    </Grid>
</UserControl>
