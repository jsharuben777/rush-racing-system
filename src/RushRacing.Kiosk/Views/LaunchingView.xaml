<UserControl x:Class="RushRacing.Kiosk.Views.LaunchingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A1A">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="🏁 RUSH RACING"
                       Foreground="#FF3333"
                       FontSize="36"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,40"/>

            <TextBlock x:Name="GameNameText"
                       Text="Loading..."
                       Foreground="White"
                       FontSize="32"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <TextBlock Text="Please wait..."
                       Foreground="#888888"
                       FontSize="20"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <!-- Simple loading animation -->
            <ProgressBar IsIndeterminate="True"
                         Width="300" Height="6"
                         Foreground="#FF3333"
                         Background="#2A2A4A"
                         BorderThickness="0"/>

        </StackPanel>
    </Grid>
</UserControl>